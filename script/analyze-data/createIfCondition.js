function createIfCondition(arr) {
    // return new Function('"use strict"; return(ind < 2)')();
    // console.log('createIfCondition');

    // console.log(arr);
    // console.log(arr.includes('3'));

    // const option = '';

    // if ( arr.includes('1') ) return new Function
}